<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/"><i class="fa fa-thumbs-o-up fa-rotate-270" aria-hidden="true"></i> 顶教程 </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

          <ul class="nav navbar-nav navbar-right">
            <li>
              <%= link_to new_tutorial_path do %><i class="fa fa-file-text-o" aria-hidden="true"></i> 写教程<% end %>
            </li>
            <% if !current_user %>
              <%= link_to("注册", new_user_registration_path, class: "btn btn-sm btn-info navbar-btn") %>
            <% elsif current_user.is_admin%>
              <%= link_to("管理员后台", admin_tutorials_path, class:"btn btn-sm btn-primary navbar-btn") %>
              <%= link_to("用户中心", edit_account_user_path(current_user), class:"btn btn-sm btn-primary navbar-btn") %>
            <% else %>
              <%= link_to("用户中心", edit_account_user_path(current_user), class:"btn btn-sm btn-primary navbar-btn") %>
            <% end %>
          </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
